/** @type {import('next').NextConfig} */
const nextConfig = { 
  // ... toda tu configuración de Next.js aquí ...
};

// ... Asegúrate de que no haya NADA más que declare 'nextConfig' ...

export default nextConfig;